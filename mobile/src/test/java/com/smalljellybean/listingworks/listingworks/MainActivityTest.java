package com.smalljellybean.listingworks.listingworks;

import com.smalljellybean.listingworks.BuildConfig;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.robolectric.RobolectricGradleTestRunner;
import org.robolectric.annotation.Config;

//@RunWith(RobolectricGradleTestRunner.class)
//@Config(constants = BuildConfig.class, sdk=21)
public class MainActivityTest {

    @Test
    public void should_get_correct_navigation_menu() {
//        MainActivity mainActivity = Robolectric.buildActivity(MainActivity.class).create().get();
//        ListView navigationListView = (ListView) mainActivity.findViewById(R.id.navigation_menu_list);
//
//        Shadows.shadowOf(navigationListView).performItemClick(4);
//        EditText username = (EditText) mainActivity.findViewById(R.id.username);

    }
}
